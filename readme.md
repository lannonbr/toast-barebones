# Toast Barebones Example site

A barebones [Toast](https://github.com/toastdotdev/toast) site without any integrations. Write preact files in `src/pages` and they'll be generated into HTML files at build-time.
